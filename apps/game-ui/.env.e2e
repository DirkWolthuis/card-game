# Environment variables for E2E tests
# This file is used during E2E test runs to enable E2E mode and set a predictable seed

# Enable E2E test mode to make the E2E test deck selectable
VITE_E2E_MODE=true

# Use a predictable seed for E2E tests
# Using "e2e-test" as a known seed for deterministic deck shuffling
VITE_GAME_SEED=e2e-test
